# Customizations to the Emacs installer

- Using systemd to start Emacs in daemon mode
- Creating desktop shortcut ~Emacs (Client)~ to open a new GUI Emacs frame.
- Alias 'e' to edit a file in an Emacs window.
